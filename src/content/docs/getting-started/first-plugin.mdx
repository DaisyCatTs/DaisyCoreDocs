---
title: First Plugin
description: Bootstrap DaisyCore in a Paper plugin.
---

# First Plugin

This is the simplest good starting point when you want DaisyCore to own your plugin runtime.

```kotlin
class MyPlugin : JavaPlugin() {
    private lateinit var daisy: DaisyPlatform

    override fun onEnable() {
        daisy =
            DaisyPlatform.create(this) {
                text()
                placeholders()
                items()
                scoreboards()
                tablists()
                commands()
                menus()
            }
    }

    override fun onDisable() {
        daisy.close()
    }
}
```

## What this bootstrap does

- creates the shared DaisyCore runtime
- creates a placeholder registry
- enables item and text helpers
- auto-loads command providers by scanning your plugin jar for `@DaisyCommandSet`
- enables menu, scoreboard, and tablist services

## Next steps

- Learn how commands are discovered: [Auto-Loaded Commands](/guides/auto-loaded-commands/)
- Learn why DaisyCore uses a runtime scan right now: [Why Commands Auto-Load](/explanation/command-auto-loading/)
- Read the command overview: [Commands](/commands/overview/)
