---
title: Damage Cause API
description: Public DaisySeries damage-cause parsing helpers.
---

import ReferenceTable from '@components/ReferenceTable.astro'

# Damage Cause API

<ReferenceTable
  filterable={false}
  rows={[
    { name: 'parse', type: 'enum', description: 'Parses a damage cause from canonical or normalized input.', required: true },
    { name: 'parseOrNull', type: 'enum', description: 'Returns null for invalid damage-cause input.', required: false },
    { name: 'key', type: 'string', description: 'Returns the canonical DaisySeries damage-cause key.', required: true },
    { name: 'displayName', type: 'string', description: 'Returns a UI-friendly damage-cause label.', required: true },
    { name: 'aliases', type: 'object', description: 'Returns the alias set for a damage cause.', required: false },
  ]}
/>
