---
title: Difficulty API
description: Public DaisySeries difficulty parsing helpers.
---

import ReferenceTable from '@components/ReferenceTable.astro'

# Difficulty API

<ReferenceTable
  filterable={false}
  rows={[
    { name: 'parse', type: 'enum', description: 'Parses a difficulty from canonical or normalized input.', required: true },
    { name: 'parseOrNull', type: 'enum', description: 'Returns null for invalid difficulty input.', required: false },
    { name: 'key', type: 'string', description: 'Returns the canonical DaisySeries difficulty key.', required: true },
    { name: 'displayName', type: 'string', description: 'Returns a UI-friendly difficulty label.', required: true },
    { name: 'aliases', type: 'object', description: 'Returns the alias set for a difficulty.', required: false },
  ]}
/>
