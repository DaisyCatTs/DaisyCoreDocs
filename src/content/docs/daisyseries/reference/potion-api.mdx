---
title: Potion API
description: Public DaisySeries potion effect parsing helpers.
---

import ReferenceTable from '@components/ReferenceTable.astro'

# Potion API

<div class="doc-lead">
  <span class="eyebrow">Reference</span>
  <h3>DaisyPotions is centered on <code>PotionEffectType</code> parsing, not brewing-item concepts.</h3>
</div>

<ReferenceTable
  filterable={false}
  rows={[
    { name: 'parse', type: 'enum', description: 'Parses a potion effect type from canonical or alias input.', required: true },
    { name: 'parseOrNull', type: 'enum', description: 'Returns null on invalid potion effect input.', required: false },
    { name: 'key', type: 'string', description: 'Returns the canonical DaisySeries potion effect key.', required: true },
    { name: 'displayName', type: 'string', description: 'Returns the user-facing effect label.', required: true },
    { name: 'aliases', type: 'object', description: 'Returns accepted aliases for the potion effect.', required: false },
  ]}
/>
