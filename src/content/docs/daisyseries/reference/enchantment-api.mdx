---
title: Enchantment API
description: Public DaisySeries enchantment parsing helpers.
---

import ReferenceTable from '@components/ReferenceTable.astro'

# Enchantment API

<div class="doc-lead">
  <span class="eyebrow">Reference</span>
  <h3>DaisyEnchantments handles canonical keys and curated aliases while staying explicit about registry-sensitive runtime behavior.</h3>
</div>

<ReferenceTable
  filterable={false}
  rows={[
    { name: 'parse', type: 'enum', description: 'Parses an enchantment from canonical or alias-driven input.', required: true },
    { name: 'parseOrNull', type: 'enum', description: 'Returns null when an enchantment key cannot be resolved.', required: false },
    { name: 'key', type: 'string', description: 'Returns the canonical enchantment key.', required: true },
    { name: 'displayName', type: 'string', description: 'Returns the user-facing enchantment label.', required: true },
    { name: 'aliases', type: 'object', description: 'Returns curated shorthand aliases such as ff or prot where supported.', required: false },
  ]}
/>
