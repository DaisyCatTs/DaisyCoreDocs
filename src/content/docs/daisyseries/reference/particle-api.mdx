---
title: Particle API
description: Public DaisySeries particle parsing helpers.
---

import ReferenceTable from '@components/ReferenceTable.astro'

# Particle API

<ReferenceTable
  filterable={false}
  rows={[
    { name: 'parse', type: 'enum', description: 'Parses a particle from canonical, lowercase, or alias-driven input.', required: true },
    { name: 'parseOrNull', type: 'enum', description: 'Returns null for invalid particle input.', required: false },
    { name: 'key', type: 'string', description: 'Returns the canonical DaisySeries particle key.', required: true },
    { name: 'displayName', type: 'string', description: 'Returns a UI-friendly particle label.', required: true },
    { name: 'aliases', type: 'object', description: 'Lists curated accepted aliases for a given particle.', required: false },
  ]}
/>
