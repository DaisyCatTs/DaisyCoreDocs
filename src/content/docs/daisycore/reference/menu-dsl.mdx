---
title: Menu DSL
description: Quick lookup for DaisyCore menu types and entrypoints.
---

import ReferenceTable from '@components/ReferenceTable.astro'

# Menu DSL

<div class="doc-lead">
  <span class="eyebrow">Reference</span>
  <h3>The inline <code>openMenu(...)</code> path is the preferred quick-flow API for most menu work.</h3>
</div>

<ReferenceTable
  rows={[
    { name: 'DaisyMenu', type: 'object', description: 'Immutable menu definition.', required: true },
    { name: 'DaisyMenuSession', type: 'object', description: 'Per-player runtime menu instance created when a menu opens.', required: true },
    { name: 'slot', type: 'object', description: 'Assigns one slot render and click contract.', required: false },
    { name: 'background', type: 'object', description: 'Applies a repeated filler item to unassigned menu slots.', required: false },
    { name: 'message', type: 'string', description: 'MiniMessage-first feedback path for menu clicks.', required: false },
    { name: 'plainMessage', type: 'string', description: 'Literal text escape hatch when you truly need raw text.', required: false },
    { name: 'closeOnClick', type: 'boolean', description: 'Closes the active menu session after click handling.', required: false },
    { name: 'pagination', type: 'object', description: 'Adds paged rendering behavior to repeated content areas.', required: false },
  ]}
/>

## Main entrypoints

```kotlin
fun menu(title: String, rows: Int = 3, block: DaisyMenuBuilder.() -> Unit): DaisyMenu
fun Player.openMenu(menu: DaisyMenu): DaisyMenuSession
fun Player.openMenu(
    title: String,
    rows: Int = 3,
    block: DaisyMenuBuilder.() -> Unit,
): DaisyMenuSession
```
