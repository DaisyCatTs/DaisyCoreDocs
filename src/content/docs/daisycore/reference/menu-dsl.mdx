---
title: Menu DSL
description: Quick lookup for DaisyCore menu types and entrypoints.
---

# Menu DSL

This page is the fast lookup for the public menu shape. It does not try to explain every runtime detail of sessions or invalidation strategy.

## Main types

- `DaisyMenu`
- `DaisyMenuSession`
- `DaisyMenuPlatform`
- `DaisyMenuClickContext`
- `DaisyMenuRenderContext`

## Main entrypoints

```kotlin
fun menu(title: String, rows: Int = 3, block: DaisyMenuBuilder.() -> Unit): DaisyMenu
fun Player.openMenu(menu: DaisyMenu): DaisyMenuSession
fun Player.openMenu(
    title: String,
    rows: Int = 3,
    block: DaisyMenuBuilder.() -> Unit,
): DaisyMenuSession
```

The inline `openMenu(title, rows) { ... }` form is the preferred quick-flow API.

`message("...")` is the MiniMessage-first string path.

`plainMessage("...")` is the explicit raw/plain escape hatch when you genuinely want literal text.

## Common builder features

- `slot(...)`
- `item(...)`
- `background(...)`
- `render { ... }`
- `onClick(...)`
- `message(...)`
- `plainMessage(...)`
- `closeOnClick()`
- `refreshOnClick(...)`
- `onOpen { ... }`
- `onClose { ... }`
- `template { ... }`
- `pagination(...)`

## Session terminology

- A `DaisyMenu` is the immutable menu definition.
- A `DaisyMenuSession` is the per-player runtime instance created after a player opens that menu.

## Read next

- Tutorial: [First Menu](/daisycore/getting-started/first-menu/)
- Concepts: [Menu Sessions](/daisycore/explanation/menu-sessions/)
