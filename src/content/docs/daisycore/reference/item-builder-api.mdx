---
title: Item Builder API
description: Quick lookup for DaisyCore item builders used across menus and inventory UI.
---

import ReferenceTable from '@components/ReferenceTable.astro'

# Item Builder API

<div class="doc-lead">
  <span class="eyebrow">Reference</span>
  <h3>The shared item layer keeps menu and inventory UI from repeating item-meta boilerplate.</h3>
</div>

<ReferenceTable
  filterable={false}
  rows={[
    { name: 'DaisyItemSpec', type: 'object', description: 'Immutable item definition produced by the builder.', required: true },
    { name: 'DaisyItemBuilder', type: 'object', description: 'Builder surface for names, lore, and item presentation.', required: true },
    { name: 'item', type: 'object', description: 'Entrypoint used to build a DaisyItemSpec from a material and builder block.', required: true },
    { name: 'name', type: 'string', description: 'Sets the display name using the DaisyCore text model.', required: false },
    { name: 'lore', type: 'object', description: 'Sets viewer-facing lore lines for the built item.', required: false },
  ]}
/>

```kotlin
fun item(material: Material, block: DaisyItemBuilder.() -> Unit): DaisyItemSpec
```
