---
title: Sidebar DSL
description: Quick lookup for DaisyCore sidebar types and entrypoints.
---

import ReferenceTable from '@components/ReferenceTable.astro'

# Sidebar DSL

<div class="doc-lead">
  <span class="eyebrow">Reference</span>
  <h3>DaisyCore sidebars are keyed so partial refreshes and diffing stay explicit instead of implicit.</h3>
</div>

<ReferenceTable
  rows={[
    { name: 'DaisySidebar', type: 'object', description: 'Immutable sidebar definition.', required: true },
    { name: 'DaisySidebarSession', type: 'object', description: 'Per-viewer runtime sidebar session.', required: true },
    { name: 'title', type: 'string', description: 'Sets the sidebar title with String, Component, or renderer overloads.', required: false },
    { name: 'line', type: 'object', description: 'Adds a keyed line renderer to the sidebar.', required: false },
    { name: 'lineIf', type: 'object', description: 'Adds a keyed line only when the supplied visibility condition passes.', required: false },
    { name: 'invalidate', type: 'object', description: 'Refreshes one or more keyed lines without a full sidebar rebuild.', required: false },
    { name: 'invalidateAll', type: 'object', description: 'Triggers a full keyed refresh cycle for the current session.', required: false },
    { name: 'refreshNow', type: 'object', description: 'Immediately recomputes and sends the current sidebar state.', required: false },
  ]}
/>
