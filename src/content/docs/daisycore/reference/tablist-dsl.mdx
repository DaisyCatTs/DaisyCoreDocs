---
title: Tablist DSL
description: Quick lookup for DaisyCore tablist types and entrypoints.
---

import ReferenceTable from '@components/ReferenceTable.astro'

# Tablist DSL

<div class="doc-lead">
  <span class="eyebrow">Reference</span>
  <h3>The current public tablist API is intentionally header and footer focused.</h3>
</div>

<ReferenceTable
  rows={[
    { name: 'DaisyTablist', type: 'object', description: 'Immutable tablist definition for one viewer-facing session.', required: true },
    { name: 'header', type: 'string', description: 'Sets the header using String, Component, or renderer overloads.', required: false },
    { name: 'footer', type: 'string', description: 'Sets the footer using String, Component, or renderer overloads.', required: false },
    { name: 'headerLang', type: 'string', description: 'Resolves a shared language key through the installed message source.', required: false },
    { name: 'footerLang', type: 'string', description: 'Resolves a shared language key for the footer.', required: false },
    { name: 'refreshNow', type: 'object', description: 'Immediately recomputes and sends the current header and footer.', required: false },
    { name: 'invalidate', type: 'object', description: 'Compatibility alias for a full refresh of the tablist session.', required: false },
  ]}
/>
