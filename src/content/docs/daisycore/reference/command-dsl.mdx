---
title: Command DSL
description: Quick lookup for DaisyCore command types and DSL entrypoints.
---

import ReferenceTable from '@components/ReferenceTable.astro'

# Command DSL

<div class="doc-lead">
  <span class="eyebrow">Reference</span>
  <h3>Use this page to scan the command surface quickly. The runtime explanation lives elsewhere.</h3>
</div>

<ReferenceTable
  rows={[
    { name: 'DaisyCommand', type: 'object', description: 'Immutable command model returned by the command DSL.', required: true },
    { name: 'DaisyCommandGroup', type: 'object', description: 'The clean default container for command trees discovered at startup.', required: true },
    { name: 'DaisyCommandProvider', type: 'object', description: 'Lower-level provider shape when you need custom command exposure logic.', required: false },
    { name: 'permission', type: 'string', description: 'Sets the execution permission for a command or subcommand.', required: false },
    { name: 'enabled', type: 'boolean', description: 'Startup-time inclusion filter, not a runtime permission check.', required: false },
    { name: 'ignore', type: 'boolean', description: 'Explicitly removes a command node before registration.', required: false },
    { name: 'player', type: 'object', description: 'Player-only executor block with player-aware reply helpers.', required: false },
    { name: 'console', type: 'object', description: 'Console-only executor block for console-targeted execution.', required: false },
  ]}
/>

## Main entrypoints

```kotlin
fun command(name: String, block: DaisyCommandBuilder.() -> Unit): DaisyCommand
abstract class DaisyCommandGroup(
    block: DaisyCommandGroupBuilder.() -> Unit,
)
```
