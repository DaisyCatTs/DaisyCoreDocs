---
title: Which product do I need?
description: Three products, one ecosystem. Pick the layer that owns your problem first.
---

import CrossProductCallout from '@components/CrossProductCallout.astro'
import DecisionMatrix from '@components/DecisionMatrix.astro'

# Which product do I need?

<div class="doc-lead">
  <span class="eyebrow">Decision Matrix</span>
  <h3>Three products, one ecosystem. Here's how to pick.</h3>
  <p>Choose the product that owns the problem you are actually solving first, not the one with the most features.</p>
</div>

<DecisionMatrix
  scenarios={[
    {
      goal: 'I need commands, menus, sidebars, tablists, and one runtime owner for the feature.',
      product: 'core',
      startHref: '/daisycore/getting-started/install/',
    },
    {
      goal: 'I need canonical values and aliases for materials, sounds, enchantments, flags, or potions.',
      product: 'series',
      startHref: '/daisyseries/getting-started/install/',
    },
    {
      goal: 'I need typed YAML, reload safety, and clean config-backed text ownership.',
      product: 'config',
      startHref: '/daisyconfig/getting-started/install/',
    },
  ]}
/>

<p>The Daisy suite has three separate ownership layers: DaisyCore owns runtime systems, DaisySeries owns canonical values, and DaisyConfig owns typed YAML plus shared config state. The dependency chain still matters, but the first question is which product actually owns your problem.</p>

<CrossProductCallout
  product="core"
  label="Use DaisyCore when runtime systems are the problem"
  href="/daisycore/"
  description="If the feature lives in commands, menus, sidebars, or tablists, DaisyCore is the default entrypoint."
/>

<CrossProductCallout
  product="series"
  label="Use DaisySeries when values are the problem"
  href="/daisyseries/"
  description="If you are about to write alias maps or lowercase enum serializers, start here."
/>

<CrossProductCallout
  product="config"
  label="Use DaisyConfig when YAML ownership is the problem"
  href="/daisyconfig/"
  description="If reload safety and config shape are the pain point, DaisyConfig should own that layer."
/>

<div class="premium-callout info">
  <h3>Start with DaisyCore unless you have a specific reason not to</h3>
  <p>Most plugin authors hit the runtime problem first. Only start lower in the chain when parsing or config ownership is clearly the real issue.</p>
</div>
