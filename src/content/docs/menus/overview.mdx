---
title: Menus Overview
description: DaisyCore menus use immutable menu definitions and session-based runtime state.
---

# Menus Overview

DaisyCore menus keep the external usage compact while moving the runtime complexity into the library.

## Shape

```kotlin
val menu =
    menu("Skyblock", rows = 3) {
        slot(13) {
            item(Material.GRASS_BLOCK) {
                name = "Your Island"
            }
        }
    }

player.openMenu(menu)
```

## What you get

- immutable menu definitions
- per-player sessions
- targeted invalidation
- pagination and template helpers
- runtime safety around top-inventory interactions

## Learn next

- API reference: [Menu DSL](/reference/menu-dsl/)
- Migration notes: [Migrate from DaisyMenu](/migration/daisymenu/)
