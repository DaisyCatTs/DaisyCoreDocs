---
title: Reload Handles
description: Use DaisyConfigHandle and DaisyReloadResult safely.
---

# Reload Handles

Main types:

- `DaisyConfigHandle<T>`
- `DaisyReloadResult.Success`
- `DaisyReloadResult.Failure`

`current` always points at the last valid typed config.

